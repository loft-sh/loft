suite: rbac agent role test
templates:
  - templates/rbac/agent/role.yaml

tests:
  - it: does not generate a role with default values
    asserts:
      - hasDocuments:
          count: 0
  - it: does not generate a role with agentOnly set to true
    set:
      agentOnly: true
    asserts:
      - hasDocuments:
          count: 0
  - it: generates a role with agentOnly, leastPrivilegeMode.enabled and leastPrivilegeMode.role.enabled set to true
    set:
      agentOnly: true
      leastPrivilegeMode:
        enabled: true
        clusterRole:
          enabled: true
    asserts:
      - hasDocuments:
          count: 1
      - equal:
          path: kind
          value: Role
      - equal:
          path: metadata.name
          value: vcluster-platform-agent
  - it: allows to override the role rules
    set:
      agentOnly: true
      leastPrivilegeMode:
        enabled: true
        role:
          enabled: true
          overwriteRules:
            - apiGroups: [ "testgroup" ]
              resources: [ "testresources" ]
              verbs: [ "testverb" ]
    asserts:
      - hasDocuments:
          count: 1
      - equal:
          path: kind
          value: Role
      - equal:
          path: rules
          value:
            - apiGroups: [ "testgroup" ]
              resources: [ "testresources" ]
              verbs: [ "testverb" ]
  - it: allows adding extra rules
    set:
      agentOnly: true
      leastPrivilegeMode:
        enabled: true
        role:
          enabled: true
          extraRules:
            - apiGroups: [ "extragroup" ]
              resources: [ "extraresources" ]
              verbs: [ "extreaverb" ]
    asserts:
      - hasDocuments:
          count: 1
      - equal:
          path: kind
          value: Role
      - contains:
          path: rules
          content:
            apiGroups: [ "extragroup" ]
            resources: [ "extraresources" ]
            verbs: [ "extreaverb" ]
