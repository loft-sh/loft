```bash
loft import vcluster [vcluster-name] \
  --cluster [cluster-name] \
  --namespace [vcluster-namespace] \
  --project [project-name]
```

:::tip List Legacy Virtual Clusters
You can list legacy virtual clusters via the cli by running the `loft list vclusters
--show-legacy` command.
:::