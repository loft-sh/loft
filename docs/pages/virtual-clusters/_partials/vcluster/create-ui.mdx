import Flow, { Step } from '@site/src/components/Flow'
import NavStep from '@site/src/components/NavStep'
import Button from '@site/src/components/Button'
import Label from '@site/src/components/Label'

import CreateSpaceStep1 from '@site/static/media/ui/screenshots/auth/login/login.png'
import CreateSpaceStep2 from '@site/static/media/ui/screenshots/spaces/create-space/button-open-drawer.png'


<Flow id="create-vcluster-ui">
  <Step>
    Go to the <NavStep>Virtual Clusters</NavStep> view using the menu on the left
  </Step>
  <Step>
    Click on the <Button>Create Virtual Cluster</Button> button
  </Step>
  <Step>
    Use the field <Label>Display Name</Label> to define the name of this virtual cluster and optionally specify other settings
  </Step>
  <Step image="">
    Click on the <Button>Create</Button> button at the very bottom
  </Step>
  <Step image="">

Retrieve a kube-context for this virtual cluster using Loft CLI:
```bash
loft use vcluster [vcluster-name]
```

  </Step>
</Flow>