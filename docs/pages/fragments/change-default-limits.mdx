import Highlight from '@site/src/components/Highlight/Highlight';

DevSpace Cloud lets you define default limits for each cluster. These limits are automatically used for each cluster user you create. You can change them via <Highlight backgroundColor="#8fa6b2">Clusters 🡆 [cluster-name] 🡆 Limits</Highlight>

<figure class="frame">
  <img src="/cloud/img/ui-limits-default.gif" alt="Change Default Limits" />
  <figcaption>DevSpace Cloud UI - Change Default Limits</figcaption>
</figure>

:::note New Cluster Users
When a new cluster user is added (by clicking on the invite link), DevSpace Cloud copies the default limits for this cluster to this user.
:::