import Flow, { Step } from '@site/src/components/Flow'
import NavStep from '@site/src/components/NavStep'
import Button from '@site/src/components/Button'
import Label from '@site/src/components/Label'
import Input from '@site/src/components/Input'
import Expander from '@site/src/components/Expander'

<Flow id="audit-levels-ui">
  <Step>
    Go to the <NavStep>Admin > Config</NavStep> view using the menu on the left.
  </Step>
  <Step>
    In the input field that appears, enter the following config:

```yaml
audit:
  enabled: true
  level: 1
```

  </Step>
  <Step>
    Click on the <Button>Apply</Button> button and wait until Loft has restarted.
  </Step>
</Flow>