When creating a space from the Loft CLI, you will need to provide the name of the
project in which to deploy the space, and the name of template to use:

```bash
loft create space [vcluster-name] --project [project-name] --template [template-name]
```
