import CodeBlock from '@theme/CodeBlock';

<p>To wake up a sleeping {props.name} using Loft CLI, run:</p>
<CodeBlock>
loft wakeup {props.name === "virtual cluster" ? "vcluster" : "space"} [name]
</CodeBlock>