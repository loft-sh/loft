```bash
loft import space [space-name] \
  --cluster [cluster-name] \
  --project [project-name]
```

:::tip List Legacy Spaces
You can list legacy spaces via the cli by running the `loft list spaces
--show-legacy` command.
:::